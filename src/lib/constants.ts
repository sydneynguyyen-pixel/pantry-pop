// Shared cap on both how many recipes can be active per box type (usePoolStore) and how
// many slots a shelf can draw (shelfRestock) — kept in one place so the two can't diverge.
export const MAX_ACTIVE_RECIPES = 8
